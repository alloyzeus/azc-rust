package {{$PKG_NAME}}

import (
	{{$AZCORE_PKG}} "{{$AZCORE_IMPORT}}"
)

// TODO: get the docs from the source
type {{$TYPE_NAME}} {{$PRIMITIVE_TYPE_NAME}}

var _ {{$AZCORE_PKG}}.ValueObject = {{$TYPE_NAME}}{}
